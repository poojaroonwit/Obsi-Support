const { requireAgent } = require('../../../../lib/auth');
const { addAgentMessage, getTicket } = require('../../../../lib/repository');
export default async function handler(req,res){const session=requireAgent(req,res);if(!session)return;if(req.method!=='POST')return res.status(405).json({success:false,message:'Method not allowed'});try{const ok=await addAgentMessage({organizationId:session.organizationId,ticketId:req.query.id,session,body:req.body?.body,isInternal:Boolean(req.body?.isInternal)});if(!ok)return res.status(404).json({success:false,message:'Ticket not found'});return res.status(201).json({success:true,ticket:await getTicket(session.organizationId,req.query.id)});}catch(error){return res.status(400).json({success:false,message:error.message||'Unable to add message.'});}}
