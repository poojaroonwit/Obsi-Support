const { requireAgent } = require('../../../../lib/auth');
const { getTicket, updateTicket } = require('../../../../lib/repository');
export default async function handler(req,res){const session=requireAgent(req,res);if(!session)return;try{if(req.method==='GET'){const ticket=await getTicket(session.organizationId,req.query.id);if(!ticket)return res.status(404).json({success:false,message:'Ticket not found'});return res.json({success:true,ticket});}if(req.method==='PATCH'){const ticket=await updateTicket({organizationId:session.organizationId,ticketId:req.query.id,patch:req.body||{}});if(!ticket)return res.status(404).json({success:false,message:'Ticket not found'});return res.json({success:true,ticket});}res.setHeader('Allow','GET, PATCH');return res.status(405).json({success:false,message:'Method not allowed'});}catch(error){console.error(error);return res.status(400).json({success:false,message:error.message||'Ticket operation failed.'});}}
