import Link from 'next/link';
import { useRouter } from 'next/router';

export default function AppShell({ session, children }) {
  const router = useRouter();
  const active = (path) => router.pathname === path ? 'nav-item active' : 'nav-item';
  return <div className="app-bg"><aside className="sidebar"><Link href="/inbox" className="product-brand"><span className="product-mark">S</span><span><b>Obsi</b> Support</span></Link><nav className="sidebar-nav" aria-label="Support navigation"><Link className={active('/inbox')} href="/inbox"><span>▣</span><span>Inbox</span></Link><Link className="nav-item" href="/inbox?status=open"><span>◴</span><span>Open tickets</span></Link><Link className="nav-item" href="/inbox?status=pending"><span>◌</span><span>Pending</span></Link><Link className="nav-item" href="/inbox?status=resolved"><span>✓</span><span>Resolved</span></Link><Link className={active('/routing')} href="/routing"><span>⇢</span><span>Routing</span></Link><Link className={active('/macros')} href="/macros"><span>✦</span><span>Macros</span></Link></nav><div className="sidebar-spacer"/><a className="nav-item muted" href={`/request/${session.organizationSlug || ''}`} target="_blank" rel="noreferrer"><span>↗</span><span>Request portal</span></a><div className="user-block"><span className="avatar">{(session.name || session.email || 'U').slice(0,1).toUpperCase()}</span><div><strong>{session.name || session.email}</strong><small>{session.organizationName || session.organizationSlug}</small></div><a href="/api/auth/logout" aria-label="Sign out">↪</a></div></aside><main className="app-main">{children}</main></div>;
}
